# Database Configuration
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=newsletter

# Connection string ที่ sqlx ใช้ DATABASE_URL="postgres://postgres:password@localhost:5432/newsletter"

DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}